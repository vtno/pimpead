{
  "scripts": {
    "build": "esbuild --bundle app.ts --outfile=out/app.js",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^28.1.1",
    "esbuild": "^0.14.42",
    "jest": "^28.1.1",
    "jest-environment-jsdom": "^28.1.1",
    "ts-jest": "^28.0.4"
  }
}
